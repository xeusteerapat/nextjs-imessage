import * as React from 'react';

interface IFeedWrapperProps {}

const FeedWrapper: React.FunctionComponent<IFeedWrapperProps> = props => {
  return (
    <div>
      <h1>FeedWrapper</h1>
    </div>
  );
};

export default FeedWrapper;
